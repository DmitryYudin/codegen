#
# We install from supper-user account to:        /usr/local/bin
# But '$SUDO_USER' might have same binaries in:  ~/.local/bin
# Thus, reported as 'well installed versions' are not the same as used by '$USER'
# Can we overcome this somehow?
#

NODE_VERSION=v18.20.3

#JSON_SCHEMA_TO_TYPESCRIPT_VERSION=11.0.3 # ok on Windows, other versions has path related issues
JSON_SCHEMA_TO_TYPESCRIPT_VERSION=14.1.0 # not verified on Winows

